@require 'global-variables.styl'

/* == PHONE RING EFFECT == */

@keyframes ring
  from
    transform: rotate(10deg) scale(1)
  50%
    transform rotate(-10deg) scale(1.1)
  to
    transform rotate(10deg) scale(1)

